import Bar from "@components/barRoutes/Bar.vue";
import Foo from "@components/tempRoutes/Foo.vue";

const routes = [
    { path: '/foo', component: Foo },
    { path: '/bar', component: Bar }
]

export default routes